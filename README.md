# PreLoved Baby

PHP e-commerce site for preloved baby products.

## Requirements

- PHP 8+
- MySQL

## Setup

1. Copy config:
   `config/config.example.php → config/config.php`

2. Start MySQL

3. Run server:

```bash
./start-server.sh
```
